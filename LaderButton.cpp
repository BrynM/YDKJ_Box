#include "Arduino.h"
#include "LadderButton.h"

LadderButton::LadderButton (int onPin) {
  pin = onPin;
}

int LadderButton::match_value(char *intoVar) {
  
}

/*
typedef struct ladderButton {
  keystroke buttons[6];
} ladderButton;
*/

